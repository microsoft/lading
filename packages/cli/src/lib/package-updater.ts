import { PackageUpdater } from "./types";
export class PackageUpdaterImpl implements PackageUpdater {
  addDevDependency(arg0: string): Promise<void> {
    throw new Error("Method not implemented.");
  }
}
